import 'package:flutter/material.dart';

Color background = const Color(0xFF121212);
Color overlay = const Color(0xFFFFFFFF);
Color topbar = ElevationOverlay.applySurfaceTint(background, overlay, 5);
Color sidebar = ElevationOverlay.applySurfaceTint(background, overlay, 1);
Color widget = ElevationOverlay.applySurfaceTint(background, overlay, 5);
Color light = const Color(0xFFFFFFFF);
Color accent = const Color(0xFFbf5700);
